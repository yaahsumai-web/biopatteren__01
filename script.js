const box=document.getElementById("messages"),form=document.getElementById("form"),input=document.getElementById("input"),clear=document.getElementById("clear"),status=document.getElementById("status");let messages=[];function add(role,text){const m=document.createElement("div");m.className=`msg ${role}`;const b=document.createElement("div");b.className="bubble";b.textContent=text;m.appendChild(b);box.appendChild(m);box.scrollTop=box.scrollHeight;return m}async function health(){try{const r=await fetch("/api/health"),d=await r.json();status.textContent=d.configured?"AI online":"Add Gemini API key in .env"}catch{status.textContent="Server offline"}}form.addEventListener("submit",async e=>{e.preventDefault();let text=input.value.trim();if(!text)return;messages.push({role:"user",content:text});add("user",text);input.value="";const wait=add("assistant","Checking warehouse data...");try{const r=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({messages})});const d=await r.json();wait.remove();if(!r.ok)throw Error(d.error||"Request failed");add("assistant",d.reply);messages.push({role:"assistant",content:d.reply})}catch(err){wait.remove();add("assistant","Error: "+err.message)}});clear.onclick=()=>{messages=[];box.innerHTML="";add("assistant","Chat cleared. Ask me about stock, product locations, capacity, incoming goods, or outgoing orders.")};health();